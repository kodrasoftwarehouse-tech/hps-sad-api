spring:
  datasource:
    url: jdbc:h2:mem:vilanova
    driver-class-name: org.h2.Driver
    username: vilanova
    password:
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect